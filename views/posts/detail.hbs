<div class="m-l-24">

    <a href="/" class="back-to-home">
        <i class="fa-solid fa-chevron-left"></i>
        Return
    </a>
    <div class="container">
        <div class="row clearfix d-flex justify-content-center align-items-center">
            <div class="col-lg-8 col-md-12">
                <div class="card single_post">
                    <div class="body">
                        <h3>Title: {{data.title}}</h3>
                        <h5>Description: {{data.description}}</h5>
                        <p>Content: {{{ data.content }}}</p>
                    </div>
                </div>
                
                <div class="card">
                    <div class="header">
                        <h2>Comments</h2>
                    </div>
                    <div class="body commnet-body">
                        <ul class="comment-reply list-unstyled">
                            {{#each data.comments}}
                            <li class="row clearfix">
                                <div class="icon-box col-md-2 col-4">
                                    <img class="img-fluid img-thumbnail" src="https://i.gifer.com/XOsX.gif" alt="Awesome Image">
                                </div>
                                <div class="text-box col-md-10 col-8 p-l-0 p-r0">
                                    <h5 class="m-b-0">Guest</h5>
                                    <p class="m-b-0">{{this.comment}}</p>
                                    <p class="m-b-0">Date: {{this.formattedDate}}</p>
                                </div>
                            </li>
                            {{/each}}
                        </ul>
                    </div>
                </div>
                {{!-- End comment section --}}
                <div class="card">
                    <div class="header">
                        <h2>Leave a reply</h2>
                        <span>Your email address will not be published</span>
                    </div>
                    <div class="body">
                        <div class="comment-form">
                            <form class="row clearfix" action="/post/{{data.id}}/postComment" method="POST">

                                <div class="col-sm-12">
                                    <input type="hidden" id="id" name="id" value="{{ data.id }}">
                                    <div class="form-group">
                                        <textarea rows="4" class="form-control no-resize"
                                            placeholder="Please type what you want..."
                                            name="comment"></textarea>
                                    </div>
                                    <button type="submit" class="btn btn-block btn-primary">SUBMIT</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>


</div>